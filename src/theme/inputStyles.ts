import { defineRecipe } from "@chakra-ui/react";

export const Input = defineRecipe({
  base: {
    display: "block",
    color: "text.primary",
    bg: "surface.input",
    borderRadius: "10px",
    width: "300px"
  },

})